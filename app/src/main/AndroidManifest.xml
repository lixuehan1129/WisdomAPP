<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wisdompark19">

    <!-- 允许联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取GSM（2g）、WCDMA（联通3g）等网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 获取wifi网络状态的信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 保持CPU 运转，屏幕和键盘灯有可能是关闭的,用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 获取sd卡写的权限，用于文件上传和下载 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许读取手机状态 用于创建BmobInstallation -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 允许手机振动 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 允许挂载和反挂载文件系统可移动存储 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 允许使用相机 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Permissions required for GCM -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <!-- 这个权限用于进行网络定位-->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- 这个权限用于访问GPS定位-->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位-->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <!---->
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <!---->
    <uses-permission android:name="android.permission.WRITE_SETTINGS"/>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:hardwareAccelerated="true">
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="bbzCzxGHldVGCPxiDgVd4FiCuDrryQ3E" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main.PayActivity"></activity>
        <activity android:name=".Main.CodeActivity"></activity>
        <activity android:name=".Main.MapActivity"></activity>
        <activity android:name=".Main.PeripheralActivity"></activity>
        <activity android:name=".Repair.RepairActivity"></activity>
        <activity android:name=".Main.ShopActivity"></activity>
        <activity android:name=".Repair.RepairMakeActivity"></activity>
        <activity android:name=".Repair.RepairCheckActivity"></activity>
        <activity android:name=".Society.SocietyNewMessagePage"></activity>
        <activity android:name=".Society.SocietyFindPageActivity"></activity>
        <activity android:name=".Main.ShopPageActivity"></activity>
        <activity android:name=".Mine.MineLoginActivity"></activity>
        <activity android:name=".Mine.MineRegistActivity"></activity>


        <service android:name="com.baidu.location.f"
            android:enabled="true"
            android:process=":remote">
        </service>
    </application>

</manifest>